/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['github.com', 'sungaikitatanggungjawabkita.web.app', 'restorifytubess.streamlit.app'],
  },
}

module.exports = nextConfig